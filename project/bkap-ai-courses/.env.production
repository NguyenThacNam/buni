# Chỉ áp dụng khi chạy `npm run build`.
# Bản build được copy vào src/main/resources/static của Spring Boot,
# FE và API cùng origin nên dùng đường dẫn tương đối, không cần CORS.
REACT_APP_API_BASE_URL=/api/v1
